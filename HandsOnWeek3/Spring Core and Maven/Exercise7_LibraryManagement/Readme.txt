Exercise 7 – Constructor and Setter Injection
This project demonstrates both constructor and setter based dependency injection using Spring XML.

BookService receives 'libraryName' via constructor
BookRepository is injected via setter

Both injections are configured in applicationContext.xml
